Each executable target (launcher, game, server, tool) has a paired pair of files: an .ico binary icon and a corresponding .rc resource script that declares it as the APPICON resource. The .rc files are minimal one-line declarations mapping the APPICON identifier to the matching .ico file, following the Windows Resource Compiler convention. There is no shared resource manifest — each application owns its own icon resource independently.